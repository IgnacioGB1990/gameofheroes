const express = require("express");
const router = express.Router();

/* GET home page */
/* La hemos quitado según video de Heroku
router.get("/", (req, res, next) => {
  res.json({ status: "Welcome" });
});

*/

module.exports = router;
